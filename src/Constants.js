const ITEM_STATUS = {
    available: "available",
    scheduled: "scheduled",
}

const CHECKBOX_STATES = {
    checked: "checked",
    unchecked: "unchecked",
    indeterminate: "indeterminate",
}

export { ITEM_STATUS, CHECKBOX_STATES };
